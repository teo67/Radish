d logtime p t() {
    d ti p time.convert(time.now(), time.zones.PDST)
    d hours p time.hours(ti)
    d str p "The time is '"'hours % 12'".padLeft("0", 2)':'"'time.minutes(ti)'".padLeft("0", 2)':'"'time.seconds(ti)'".padLeft("0", 2)' 'hours < 12 ? "AM" , "PM"' on '"'time.months(ti) + 1'".padLeft("0", 2)'/'"'time.days(ti) + 1'".padLeft("0", 2)'/'time.years(ti)'"
    system.writeFile(system.getDir(PATH) + "/logfile.json", JSON.to(str))
}
logtime()